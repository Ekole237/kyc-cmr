import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { router } from "expo-router";
import { useMemo, useState } from "react";
import { Alert, KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";

import { PrimaryButton } from "@/components/kyc-ui";
import { ScreenContainer } from "@/components/screen-container";
import { useKycTheme } from "@/hooks/use-kyc-theme";
import { useKyc } from "@/lib/kyc-store";
import type { DocumentType } from "@/shared/kyc";

const documentOptions: DocumentType[] = ["CNI", "Passeport", "Carte de séjour"];
const cities = ["Yaoundé", "Douala", "Bafoussam", "Bamenda", "Garoua", "Autre"];

export default function NewCaseScreen() {
  const { createCase } = useKyc();
  const theme = useKycTheme();
  const [fullName, setFullName] = useState("");
  const [phone, setPhone] = useState("+237 ");
  const [city, setCity] = useState("Yaoundé");
  const [documentType, setDocumentType] = useState<DocumentType>("CNI");
  const [purpose, setPurpose] = useState("Ouverture de compte");
  const valid = useMemo(() => fullName.trim().length > 2 && phone.replace(/\D/g, "").length >= 9 && purpose.trim().length > 2, [fullName, phone, purpose]);
  const create = () => { if (!valid) { Alert.alert("Informations incomplètes", "Renseignez le nom, un téléphone camerounais et la finalité du dossier."); return; } const caseData = createCase({ fullName: fullName.trim(), phone: phone.trim(), city, documentType, purpose: purpose.trim() }); router.replace({ pathname: "/cases/[id]", params: { id: caseData.id } }); };
  const inputTheme = { borderColor: theme.border, backgroundColor: theme.surface, color: theme.foreground };

  return <ScreenContainer edges={["top", "bottom", "left", "right"]}><KeyboardAvoidingView behavior={Platform.OS === "ios" ? "padding" : undefined} style={styles.flex}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false}><View style={styles.nav}><Pressable onPress={() => router.back()} style={({ pressed }) => [styles.backButton, { backgroundColor: theme.surfaceAccent }, pressed && styles.pressed]}><MaterialIcons name="arrow-back" size={21} color={theme.primary} /></Pressable><Text style={[styles.navTitle, { color: theme.foreground }]}>Nouveau dossier</Text><View style={styles.navSpacer} /></View><View style={styles.progress}><View style={[styles.progressActive, { backgroundColor: theme.primary }]} /><View style={[styles.progressMuted, { backgroundColor: theme.borderSoft }]} /><View style={[styles.progressMuted, { backgroundColor: theme.borderSoft }]} /></View><Text style={[styles.eyebrow, { color: theme.muted }]}>ÉTAPE 1 SUR 3</Text><Text style={[styles.title, { color: theme.foreground }]}>Créer le dossier</Text><Text style={[styles.description, { color: theme.muted }]}>Enregistrez uniquement les informations nécessaires pour démarrer le parcours de vérification.</Text><Text style={[styles.label, { color: theme.foreground }]}>Nom complet</Text><TextInput value={fullName} onChangeText={setFullName} placeholder="Ex. Marie Ndom" placeholderTextColor={theme.muted} style={[styles.input, inputTheme]} autoCapitalize="words" returnKeyType="next" /><Text style={[styles.label, { color: theme.foreground }]}>Numéro de téléphone</Text><TextInput value={phone} onChangeText={setPhone} placeholder="+237 6XX XXX XXX" placeholderTextColor={theme.muted} style={[styles.input, inputTheme]} keyboardType="phone-pad" returnKeyType="next" /><Text style={[styles.label, { color: theme.foreground }]}>Ville</Text><View style={styles.chipWrap}>{cities.map((option) => <Pressable key={option} onPress={() => setCity(option)} style={({ pressed }) => [styles.chip, { borderColor: city === option ? theme.primary : theme.border, backgroundColor: city === option ? theme.surfaceAccent : theme.surface }, pressed && styles.pressed]}><Text style={[styles.chipText, { color: city === option ? theme.primary : theme.muted }]}>{option}</Text></Pressable>)}</View><Text style={[styles.label, { color: theme.foreground }]}>Document présenté</Text><View style={styles.optionGroup}>{documentOptions.map((option) => <Pressable key={option} onPress={() => setDocumentType(option)} style={({ pressed }) => [styles.option, { borderColor: documentType === option ? theme.primary : theme.border, backgroundColor: documentType === option ? theme.surfaceSubtle : theme.surface }, pressed && styles.pressed]}><View style={[styles.radio, { borderColor: documentType === option ? theme.primary : theme.muted }]}>{documentType === option ? <View style={[styles.radioCenter, { backgroundColor: theme.primary }]} /> : null}</View><Text style={[styles.optionText, { color: theme.foreground }]}>{option}</Text></Pressable>)}</View><Text style={[styles.label, { color: theme.foreground }]}>Finalité de la vérification</Text><TextInput value={purpose} onChangeText={setPurpose} placeholder="Ex. ouverture de compte" placeholderTextColor={theme.muted} style={[styles.input, styles.textarea, inputTheme]} multiline textAlignVertical="top" /><View style={[styles.info, { backgroundColor: theme.surfaceNotice }]}><MaterialIcons name="info-outline" size={19} color={theme.primary} /><Text style={[styles.infoText, { color: theme.isDark ? "#C4D9F2" : "#385575" }]}>La démonstration stocke ces informations localement. N’utilisez pas de données personnelles réelles avant l’intégration d’un environnement sécurisé.</Text></View></ScrollView><View style={[styles.footer, { borderTopColor: theme.border, backgroundColor: theme.background }]}><PrimaryButton label="Continuer vers les preuves" onPress={create} disabled={!valid} /></View></KeyboardAvoidingView></ScreenContainer>;
}

const styles = StyleSheet.create({
  flex: { flex: 1 }, content: { padding: 20, paddingBottom: 30 }, nav: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, backButton: { width: 42, height: 42, borderRadius: 14, alignItems: "center", justifyContent: "center" }, navTitle: { fontSize: 16, fontWeight: "900" }, navSpacer: { width: 42 }, progress: { flexDirection: "row", gap: 6, marginTop: 26, marginBottom: 18 }, progressActive: { height: 5, flex: 1, borderRadius: 99 }, progressMuted: { height: 5, flex: 1, borderRadius: 99 }, eyebrow: { fontSize: 11, fontWeight: "900", letterSpacing: 1.15 }, title: { fontSize: 28, lineHeight: 35, fontWeight: "900", marginTop: 4 }, description: { fontSize: 14, lineHeight: 20, marginTop: 6, marginBottom: 24 }, label: { fontSize: 13, lineHeight: 18, fontWeight: "900", marginBottom: 7, marginTop: 17 }, input: { minHeight: 52, borderWidth: 1, borderRadius: 14, paddingHorizontal: 14, fontSize: 15 }, textarea: { minHeight: 90, paddingVertical: 12 }, chipWrap: { flexDirection: "row", gap: 8, flexWrap: "wrap" }, chip: { paddingHorizontal: 12, paddingVertical: 9, borderRadius: 999, borderWidth: 1 }, chipText: { fontSize: 12, fontWeight: "700" }, optionGroup: { gap: 8 }, option: { minHeight: 48, flexDirection: "row", alignItems: "center", gap: 10, paddingHorizontal: 13, borderRadius: 14, borderWidth: 1 }, radio: { height: 19, width: 19, borderRadius: 10, borderWidth: 2, alignItems: "center", justifyContent: "center" }, radioCenter: { height: 9, width: 9, borderRadius: 5 }, optionText: { fontSize: 14, fontWeight: "700" }, info: { flexDirection: "row", gap: 9, padding: 13, borderRadius: 14, marginTop: 20 }, infoText: { flex: 1, fontSize: 12, lineHeight: 17 }, footer: { borderTopWidth: 1, padding: 16 }, pressed: { opacity: 0.72 },
});
